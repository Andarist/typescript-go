narrowIntrinsicStringMappingByEquality.ts(20,9): error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"BE"' have no overlap.
narrowIntrinsicStringMappingByEquality.ts(20,28): error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"LU"' have no overlap.
narrowIntrinsicStringMappingByEquality.ts(20,47): error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"NL"' have no overlap.
narrowIntrinsicStringMappingByEquality.ts(48,9): error TS2367: This comparison appears to be unintentional because the types '`FOO-${Uppercase<string>}`' and '"foo-bar"' have no overlap.
narrowIntrinsicStringMappingByEquality.ts(68,9): error TS2367: This comparison appears to be unintentional because the types '`Foo-${string}`' and '"foo-bar"' have no overlap.


==== narrowIntrinsicStringMappingByEquality.ts (5 errors) ====
    // https://github.com/microsoft/TypeScript/issues/63724
    
    function narrowUppercase(prefix: Uppercase<string>) {
        let countryCode: "BE" | "LU" | "NL";
    
        if (prefix === "BE" || prefix === "LU" || prefix === "NL") {
            countryCode = prefix;
        }
    }
    
    function narrowLowercase(prefix: Lowercase<string>) {
        let countryCode: "be" | "lu" | "nl";
    
        if (prefix === "be" || prefix === "lu" || prefix === "nl") {
            countryCode = prefix;
        }
    
        let uppercaseCountryCode: "BE" | "LU" | "NL";
    
        if (prefix === "BE" || prefix === "LU" || prefix === "NL") {
            ~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"BE"' have no overlap.
                               ~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"LU"' have no overlap.
                                                  ~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"NL"' have no overlap.
            uppercaseCountryCode = prefix;
        }
    }
    
    function narrowCapitalize(country: Capitalize<string>) {
        let countryName: "Belgium" | "Luxembourg" | "Netherlands";
    
        if (country === "Belgium" || country === "Luxembourg" || country === "Netherlands") {
            countryName = country;
        }
    }
    
    function narrowUncapitalize(country: Uncapitalize<string>) {
        let countryName: "belgium" | "luxembourg" | "netherlands";
    
        if (country === "belgium" || country === "luxembourg" || country === "netherlands") {
            countryName = country;
        }
    }
    
    function narrowUppercasePattern(prefix: Uppercase<`foo-${string}`>) {
        let pattern: "FOO-BAR" | "FOO-BAZ";
    
        if (prefix === "FOO-BAR" || prefix === "FOO-BAZ") {
            pattern = prefix;
        }
    
        if (prefix === "foo-bar") {
            ~~~~~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`FOO-${Uppercase<string>}`' and '"foo-bar"' have no overlap.
            pattern = prefix;
        }
    }
    
    function narrowLowercasePattern(prefix: Lowercase<`FOO-${string}`>) {
        let pattern: "foo-bar" | "foo-baz";
    
        if (prefix === "foo-bar" || prefix === "foo-baz") {
            pattern = prefix;
        }
    }
    
    function narrowCapitalizePattern(prefix: Capitalize<`foo-${string}`>) {
        let pattern: "Foo-bar" | "Foo-baz";
    
        if (prefix === "Foo-bar" || prefix === "Foo-baz") {
            pattern = prefix;
        }
    
        if (prefix === "foo-bar") {
            ~~~~~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`Foo-${string}`' and '"foo-bar"' have no overlap.
            pattern = prefix;
        }
    }
    