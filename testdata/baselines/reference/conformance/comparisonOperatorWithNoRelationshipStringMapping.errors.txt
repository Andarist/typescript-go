comparisonOperatorWithNoRelationshipStringMapping.ts(3,9): error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string>' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(8,9): error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"BA"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(13,9): error TS2367: This comparison appears to be unintentional because the types 'Capitalize<string>' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(18,9): error TS2367: This comparison appears to be unintentional because the types 'Uncapitalize<string>' and '"Ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(23,9): error TS2367: This comparison appears to be unintentional because the types '`FOO-${Uppercase<string>}`' and '"FOO-ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(28,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${Lowercase<string>}`' and '"foo-BA"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(33,9): error TS2367: This comparison appears to be unintentional because the types '`Foo-${string}`' and '"foo-ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(38,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and '"Foo-ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(43,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and '"bar-c"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(48,9): error TS2367: This comparison appears to be unintentional because the types 'number | Uppercase<string>' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(58,9): error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string> | Uppercase<string>' and '"Ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(63,9): error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string> & { __brand: "uppercase"; }' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(68,9): error TS2367: This comparison appears to be unintentional because the types 'Uppercase<T>' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(73,9): error TS2367: This comparison appears to be unintentional because the types 'Uppercase<T>' and '"BC"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(91,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${T}`' and '"foo-ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(96,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${T}`' and '"foo-bc"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(100,9): error TS2367: This comparison appears to be unintentional because the types '`${BrandedString}`' and '"ba"' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(110,9): error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string>' and 'StringMappingComparisonValue.Lowercase' have no overlap.
comparisonOperatorWithNoRelationshipStringMapping.ts(120,9): error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and 'TemplateComparisonValue.Miss' have no overlap.


==== comparisonOperatorWithNoRelationshipStringMapping.ts (19 errors) ====
    function compareUppercase(foo: Uppercase<string>) {
        if (foo === "BA") {} // No error
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string>' and '"ba"' have no overlap.
    }
    
    function compareLowercase(foo: Lowercase<string>) {
        if (foo === "ba") {} // No error
        if (foo === "BA") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string>' and '"BA"' have no overlap.
    }
    
    function compareCapitalize(foo: Capitalize<string>) {
        if (foo === "Ba") {} // No error
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Capitalize<string>' and '"ba"' have no overlap.
    }
    
    function compareUncapitalize(foo: Uncapitalize<string>) {
        if (foo === "ba") {} // No error
        if (foo === "Ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uncapitalize<string>' and '"Ba"' have no overlap.
    }
    
    function compareUppercasePattern(foo: Uppercase<`foo-${string}`>) {
        if (foo === "FOO-BA") {} // No error
        if (foo === "FOO-ba") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`FOO-${Uppercase<string>}`' and '"FOO-ba"' have no overlap.
    }
    
    function compareLowercasePattern(foo: Lowercase<`FOO-${string}`>) {
        if (foo === "foo-ba") {} // No error
        if (foo === "foo-BA") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${Lowercase<string>}`' and '"foo-BA"' have no overlap.
    }
    
    function compareCapitalizePattern(foo: Capitalize<`foo-${string}`>) {
        if (foo === "Foo-ba") {} // No error
        if (foo === "foo-ba") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`Foo-${string}`' and '"foo-ba"' have no overlap.
    }
    
    function compareUncapitalizePattern(foo: Uncapitalize<`Foo-${string}`>) {
        if (foo === "foo-ba") {} // No error
        if (foo === "Foo-ba") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and '"Foo-ba"' have no overlap.
    }
    
    function compareTemplate(foo: `foo-${string}`) {
        if (foo === "foo-c") {} // No error
        if (foo === "bar-c") {} // Error
            ~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and '"bar-c"' have no overlap.
    }
    
    function compareUppercaseUnion(foo: Uppercase<string> | number) {
        if (foo === "BA") {} // No error
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'number | Uppercase<string>' and '"ba"' have no overlap.
    }
    
    function compareUppercaseUnionWithLiteral(foo: Uppercase<string> | "ba") {
        if (foo === "ba") {} // No error
    }
    
    function compareStringMappingUnion(foo: Uppercase<string> | Lowercase<string>) {
        if (foo === "BA") {} // No error
        if (foo === "ba") {} // No error
        if (foo === "Ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Lowercase<string> | Uppercase<string>' and '"Ba"' have no overlap.
    }
    
    function compareUppercaseIntersection(foo: Uppercase<string> & { __brand: "uppercase" }) {
        if (foo === "BA") {} // No error
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string> & { __brand: "uppercase"; }' and '"ba"' have no overlap.
    }
    
    function compareGenericUppercase<T extends string>(foo: Uppercase<T>) {
        if (foo === "BA") {} // No error
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uppercase<T>' and '"ba"' have no overlap.
    }
    
    function compareConstrainedGenericUppercase<T extends "ba" | "bb">(foo: Uppercase<T>) {
        if (foo === "BA") {} // No error
        if (foo === "BC") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uppercase<T>' and '"BC"' have no overlap.
    }
    
    function compareGenericUppercaseUnion<T extends string>(foo: Uppercase<T> | number) {
        if (foo === "BA") {} // No error
    }
    
    function compareGenericUppercaseIntersection<T extends string>(foo: Uppercase<T> & { __brand: "uppercase" }) {
        if (foo === "BA") {} // No error
    }
    
    type BrandedString = string & { __brand: "input" };
    
    function compareUppercaseBrandedString(foo: Uppercase<BrandedString>) {
        if (foo === "BA") {} // No error
    }
    
    function compareGenericTemplate<T extends string>(foo: `foo-${T}`) {
        if (foo === "foo-ba") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${T}`' and '"foo-ba"' have no overlap.
    }
    
    function compareConstrainedGenericTemplate<T extends "ba" | "bb">(foo: `foo-${T}`) {
        if (foo === "foo-ba") {} // No error
        if (foo === "foo-bc") {} // Error
            ~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${T}`' and '"foo-bc"' have no overlap.
    }
    
    function compareBrandedTemplate(foo: `${BrandedString}`) {
        if (foo === "ba") {} // Error
            ~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`${BrandedString}`' and '"ba"' have no overlap.
    }
    
    enum StringMappingComparisonValue {
        Uppercase = "BA",
        Lowercase = "ba",
    }
    
    function compareUppercaseEnum(foo: Uppercase<string>) {
        if (foo === StringMappingComparisonValue.Uppercase) {} // No error
        if (foo === StringMappingComparisonValue.Lowercase) {} // Error
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types 'Uppercase<string>' and 'StringMappingComparisonValue.Lowercase' have no overlap.
    }
    
    enum TemplateComparisonValue {
        Match = "foo-ba",
        Miss = "bar-ba",
    }
    
    function compareTemplateEnum(foo: `foo-${string}`) {
        if (foo === TemplateComparisonValue.Match) {} // No error
        if (foo === TemplateComparisonValue.Miss) {} // Error
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! error TS2367: This comparison appears to be unintentional because the types '`foo-${string}`' and 'TemplateComparisonValue.Miss' have no overlap.
    }
    